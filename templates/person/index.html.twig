{% extends 'base.html.twig' %}

{% block title %}Person index{% endblock %}

{% block body %}
    <h1>Tous les membres</h1>
<br>
    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Age</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for key, person in people %}
            <tr>        
                <td title="{{decorated[key].introduceMyself(person)}}">{{ person.name }}</td>
                <td>{{ person.age }}</td>
                <td>
                    <a href="{{ path('app_person_show', {'id': person.id}) }}">En savoir plus</a>
                    <a href="{{ path('app_person_edit', {'id': person.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun membre enregistré</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_person_new') }}">Ajouter un ami</a>
{% endblock %}
